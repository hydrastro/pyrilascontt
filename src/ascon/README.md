# Short TinyTapeout ASCON RTL names

These files are the TinyTapeout-shortened scaffold for the selected AEAD128 architecture.
Long generated names were renamed to `tt_ascon_*` to avoid path/name-length problems in the TinyTapeout flow.

This is still structural scaffold RTL, not the final AEAD datapath implementation.
